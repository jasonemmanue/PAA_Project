# Copie du Procfile racine — placée ici si Root Directory = "backend".
web: alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port ${PORT} --proxy-headers --forwarded-allow-ips=*
release: alembic upgrade head
