# ============================================================
# Dépendances Python du backend PAA-Traverse
# ============================================================

# --- API web ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- Base de données ---
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.2,<4.0

# --- Configuration / validation ---
pydantic-settings>=2.5,<3.0

# --- Ordonnanceur (collecte planifiée) ---
APScheduler>=3.10,<4.0

# --- Clients HTTP et cache ---
httpx>=0.27,<1.0
redis>=5.0,<6.0

# --- Traitement de données et import GPX / Excel ---
pandas>=2.2,<3.0
openpyxl>=3.1,<4.0

# --- Modèles prédictifs (P6) ---
scikit-learn>=1.5,<2.0
